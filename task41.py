#task41 regular expression

class regex:
	def __init__(self, e, empty, char, union, star, concat):
		self.e = e
		self.empty = empty
		self.char = char
		self.union = union
		self.star = star
		self.concat = concat
